% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{euclidean_alignment}
\alias{euclidean_alignment}
\title{Euclidean Alignment (EA) for EEG Trials}
\usage{
euclidean_alignment(trials, target_R = NULL)
}
\arguments{
\item{trials}{List of matrices (channels × samples) from one domain.}

\item{target_R}{Optional SPD (channels × channels). If \code{NULL}, align to identity.}
}
\value{
List of aligned trials (same shapes as inputs).
}
\description{
\code{euclidean_alignment} whitens a domain to identity (or recolors to \code{target_R})
and applies the same linear transform to each trial.
}
\details{
Each trial \eqn{X} is mapped by \eqn{X' = R_{\mathrm{target}}^{1/2} R_{\mathrm{domain}}^{-1/2} X},
where \eqn{R_{\mathrm{domain}}} is the mean covariance over trials.
}
\examples{
\dontrun{
tr <- replicate(5, matrix(rnorm(32*100), 32, 100), simplify = FALSE)
out <- euclidean_alignment(tr)
}
}
\seealso{
Other domain-adaptation: 
\code{\link{distanceSummary}()},
\code{\link{evaluate_shift}()},
\code{\link{kmm_weights}()},
\code{\link{label_shift_em}()},
\code{\link{proxy_a_distance}()}
}
\concept{domain-adaptation}
