% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels_metrics.R
\name{rbf_kernel}
\alias{rbf_kernel}
\title{RBF (Gaussian) Kernel Computation}
\usage{
rbf_kernel(x, y, sigma, standard_scale = TRUE)
}
\arguments{
\item{x}{Numeric matrix (n_x × p): observations by features.}

\item{y}{Numeric matrix (n_y × p): must have the same number of columns as \code{x}.}

\item{sigma}{Positive scalar bandwidth.}

\item{standard_scale}{Logical. If \code{TRUE} use \eqn{\gamma = 1/(2\sigma^2)};
otherwise \eqn{\gamma = 1/\sigma^2}. Default \code{TRUE}.}
}
\value{
Numeric matrix (n_x × n_y) of RBF kernel values.
}
\description{
\code{rbf_kernel} computes the Radial Basis Function (RBF) kernel between
two sets of observations \code{x} and \code{y} using
\eqn{\exp(-\gamma \|x-y\|^2)} and returns the \code{x}–\code{y} cross-block.
}
\details{
Distances are computed via the identity \eqn{\|x-y\|^2 = \|x\|^2+\|y\|^2-2x^\top y}
for numerical stability; tiny negative values are clamped to zero.
}
\examples{
\dontrun{
set.seed(123)
x <- matrix(rnorm(20), 5, 4); y <- matrix(rnorm(24), 6, 4)
K <- rbf_kernel(x, y, sigma = 1)
dim(K)
}
}
\seealso{
\code{\link{sigma_med}}, \code{\link{compute_mmd}}

Other kernels-metrics: 
\code{\link{compute_distance_matrix}()},
\code{\link{compute_energy}()},
\code{\link{compute_mmd}()},
\code{\link{compute_wasserstein}()},
\code{\link{sigma_med}()}
}
\concept{kernels-metrics}
