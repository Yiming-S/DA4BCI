% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kernels_metrics.R
\name{compute_distance_matrix}
\alias{compute_distance_matrix}
\title{Pairwise Euclidean Distance Matrix}
\usage{
compute_distance_matrix(source, target, eps = 1e-12)
}
\arguments{
\item{source}{Numeric matrix (n_s × p).}

\item{target}{Numeric matrix (n_t × p) with the same number of columns.}

\item{eps}{Numeric tolerance to clamp tiny negative squared distances to zero.}
}
\value{
Numeric matrix (n_s × n_t) of Euclidean distances.
}
\description{
\code{compute_distance_matrix} returns pairwise Euclidean distances between
rows of \code{source} and \code{target}.
}
\details{
Uses the identity \eqn{\|x-y\|^2 = \|x\|^2+\|y\|^2-2x^\top y}. Squared distances
in \((-{\tt eps},0)\) are set to 0; values below \(-{\tt eps}\) are flagged \code{NA}.
}
\examples{
\dontrun{
A <- matrix(rnorm(15), 5, 3); B <- matrix(rnorm(12), 4, 3)
D <- compute_distance_matrix(A, B); dim(D)
}
}
\seealso{
Other kernels-metrics: 
\code{\link{compute_energy}()},
\code{\link{compute_mmd}()},
\code{\link{compute_wasserstein}()},
\code{\link{rbf_kernel}()},
\code{\link{sigma_med}()}
}
\concept{kernels-metrics}
